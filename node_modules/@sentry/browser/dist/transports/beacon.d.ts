import { SentryResponse } from '@sentry/types';
import { BaseTransport } from './base';
/** `sendBeacon` based transport */
export declare class BeaconTransport extends BaseTransport {
    /**
     * @inheritDoc
     */
    sendEvent(body: string): Promise<SentryResponse>;
}
